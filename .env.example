# DeepSeek（与 OpenAI SDK / Vercel AI SDK 兼容）
DEEPSEEK_API_KEY=

# 可选，默认 deepseek-chat；也可用 deepseek-reasoner 等
# DEEPSEEK_MODEL=deepseek-chat

# 可选，默认 https://api.deepseek.com/v1（需包含 /v1，与 @ai-sdk/openai 的路径拼接一致）
# DEEPSEEK_BASE_URL=https://api.deepseek.com/v1
